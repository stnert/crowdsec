#!/usr/bin/env bash

#shellcheck disable=SC1007
THIS_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
#shellcheck disable=SC2164
cd "${THIS_DIR}"
#shellcheck disable=SC1090
. ./.environment.sh

exec "${LOCAL_DIR}/bin/crowdsec.test" -test.run "^TestRunMain$" "-test.coverprofile=${THIS_DIR}/coverage/$$.out" -test.v -- "$@"

